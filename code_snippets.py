#const res = await fetch("http://localhost:8000/ask", { method: "POST", headers: { "Content-Type": "application/json", "Authorization": Bearer ${token}, }, body: JSON.stringify({ message: prompt, context: "", chat_id, chat_subject, selected // send the square state if needed }), });



#const newMessage = { question: data.message, response: data.response,
